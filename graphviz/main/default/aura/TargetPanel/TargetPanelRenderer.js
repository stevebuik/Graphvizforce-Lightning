({
	// Your renderer method overrides go here
	/*
    afterRender : function(component, helper){
        this.superAfterRender();
        helper.handleGroupChanged(component, helper);
    },
    
    rerender : function(component, helper){
        this.superRerender();
        helper.handleGroupChanged(component, helper);
    }
    */
	
})